<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.binyamin.trainme">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_saquon_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_saquon_icon_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:targetApi="m">
        <activity android:name="._Page4_AthleteWorkout"
            android:hardwareAccelerated="true"/>
        <activity android:name="._Page3_SelectWorkout"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name="._Page2_UserDetails" />
        <activity android:name="._Page1_HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver
            android:name="com.binyamin.trainme.NetworkConnectionCheck"
            android:label="NetworkChangeReceiver" >
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>